name: event_scanner_app
description: A Flutter scanner app skeleton for event tickets
publish_to: 'none' # for private project

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # State management
  provider: ^6.0.5

  # HTTP client for API requests
  dio: ^5.9.1

  # QR Scanner (add later if needed)
#  mobile_scanner: ^3.0.0
#  qr_scanner_web: ^0.0.9
#  qrcode_scanner_dialog: ^1.0.0
  # Optional UI helpers
  cupertino_icons: ^1.0.5
  qr_code_scanner_plus: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  fonts:
    - family: Lexend
      fonts:
        - asset: assets/Lexend-VariableFont_wght.ttf
  assets:
    - assets/voltraLogo.png